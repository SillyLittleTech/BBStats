name: Weekly update activity summary

on:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:

jobs:
  update-summary:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm ci

      - name: Run summary fetch script
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          RANGE: '7d'
          # Fetch a focused window matching what we test locally (7 days). This makes CI output
          # more consistent with local runs that capture blocked events.
          MAX_FETCH_DAYS: '7'
          # Try to collect blocked events (stop once this many blocked logs are gathered). Tune as needed.
          FETCH_TARGET_BLOCKED: '50'
        run: node scripts/fetch-summary.js

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if git add public/activity-summary.json && git commit -m "chore: update activity-summary.json (weekly)"; then
            git push
          else
            echo "No changes to commit"
          fi
